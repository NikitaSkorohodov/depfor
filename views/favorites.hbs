<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>{{title}}</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <div class="container">
    <h1>{{title}}</h1>
    {{#if courses.length}}
      <ul>
        {{#each courses}}
          <li>
            <p>Курс: {{this.title}}</p>
            <p>Цена: {{this.price}} руб.</p>
            <form action="/favorites/remove" method="POST">
              <input type="hidden" name="courseId" value="{{this._id}}">
              <button type="submit">&times;</button>
            </form>
          </li>
        {{/each}}
      </ul>
    {{else}}
      <p>У вас пока нет понравившихся курсов.</p>
    {{/if}}
  </div>
</body>
</html>
